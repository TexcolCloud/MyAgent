# Separate the Knowledge Base from Memory

Operator-managed reference material belongs to the Knowledge Base, while durable facts and preferences learned from interaction belong to Memory. Memory is scoped to one Personal Agent, and model-proposed writes use a governed Tool Call that requires Approval by default. The two capabilities may share retrieval infrastructure, but remain distinct domain records because their authorship, trust, correction, and retention rules differ.
