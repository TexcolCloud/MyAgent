# Retain canonical history and derive Session Summaries

Original Session messages and Run Events remain the canonical record. When model context exceeds its threshold, the runtime derives a replaceable Session Summary for prompt assembly without deleting source history or treating the summary as Memory, preserving auditability and keeping conversation compression separate from learned facts.
