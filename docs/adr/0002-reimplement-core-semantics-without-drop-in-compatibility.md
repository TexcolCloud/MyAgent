# Reimplement core semantics without drop-in OpenClaw compatibility

The project will independently implement the essential personal Agent workflow inspired by OpenClaw, but it will not promise drop-in compatibility with OpenClaw APIs, configuration, or runtime behavior. This keeps the kernel small enough to understand and own while allowing an explicit compatibility surface to be added later where it creates concrete value.
