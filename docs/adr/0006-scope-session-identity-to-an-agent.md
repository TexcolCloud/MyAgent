# Scope Session identity to one Personal Agent

A Session is identified by the pair of Agent identity and Session Key rather than by a globally unique Session Key. This prevents accidental cross-Agent history sharing while allowing different Agents to reuse the same external conversation label.
